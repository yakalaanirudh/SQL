/*PRIMARY KEY*/

/*PRIMARY KEY IS LIKE  A UNIQUE CONSTRAINT*/ 
/*Unique constraint is nullable(only one entry)*/
/*Primary key is not nullable and it is clustered(sorted)*/
/*that is it sorts the table by primary key(smallest to largest)

/*Only 1 Primary Key per table*/
/*Primary key does not allow null(not even one)*/
/*Primary key can be added to a single field or a combination of multiple fields*/

/*SURROGATE KEY*/
/*If there is no coumn in the table that is unique we create a column that is unique it is called surrogate key*/
/*A surrogate key is a made up value with the sole purpose of uniquely identifying a row.*/
/*Usually, this is represented by an auto incrementing ID*/
/*SURROGATE KEY*/


/*PRIMARY KEY*/
