/*DEFAULT CONSTRAINTS-WHAT ARE THEY*/


/*Default Constraint is a value added to a row if no value is provided;some value(including null) can be added later if we want to*/
/*When adding a default constarint when creating a table do  use FOR Keyword to specify on which column*/
/*For this constraint we do not use () for specifying field name*/
/*We cannot have two constraints with the same name in two tables if they are in the same database*/
/*To drop a column with a default constraint drop the constraint fiest then the column*/
/*The default value for rows created prior to the creation of default constraint is null.


/*DEFAULT CONSTRAINTS-WHAT ARE THEY*/